package kirkos
/*
import "testing"
//import "fmt"
import "strconv"

type Blah struct {
	data string
}

func (b Blah) Free() {
	println("==Freeing==", b.data)
	b.data = ""
}

func TestCache(t *testing.T) {
	c := NewCache(20, 2)

	for i := 0; true; i++ {
		if (i % 3) == 0 {
			c.Set("test" + strconv.Itoa(i), Blah{data: "value" + strconv.Itoa(i)})
		} else {
			c.Set("test" + strconv.Itoa(i), "value" + strconv.Itoa(i))
		}
	}
	c.Free()
	//fmt.Println(c)
}
*/
